# @PROJECT_NAME@

Build and test with the shared editor-independent workflow:

```bash
cmake-workflow configure
cmake-workflow build
cmake-workflow test
```

Zed and Neovim expose the same commands through their task pickers.
